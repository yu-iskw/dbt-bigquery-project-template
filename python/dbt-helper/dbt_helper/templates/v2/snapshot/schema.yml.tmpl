{% import "macros.jinja" as macros -%}

{%- set documentation = '{{{{ doc("{}") }}}}'.format(model_reference_id) -%}
---
# It was generated by dbt-helper=={{ dbt_helper_version }}.
version: 2

snapshots:
  # To refer: {{ '{{ ref("%s") }}' | format(reference_id) }}
  - name: {{ reference_id }}
    description: |
      ## Overview
      Please briefly describe the table/view.
      It would be awesome to include how to create the table too.

      ## Special Notes
      Please describe points to note to use the table/view, if exists.

      ## Example Query
      ```sql
      ...
      ```

      ## Links
      * ...
      * ...

    columns:
      - name: DUMMY_COLUMN_NAME
        description: "DESCRIBE THE COLUMN"
        tests:
          # Please modify the test cases
          - unique
          - not_null
          - accepted_values:
              values: ["a", "b", "c"]